- **环境配置**：需复制 `.env.example` 为 `.env` 并填入 `DEEPSEEK_API_KEY`。
- **启动命令**：开发模式使用 `python -m uvicorn app.main:app --reload`，或通过入口脚本 `novel-serve` 启动。
- **测试运行**：使用 `pytest tests/ -v` 执行单元与集成测试，支持 `ruff check` 进行代码规范检查。