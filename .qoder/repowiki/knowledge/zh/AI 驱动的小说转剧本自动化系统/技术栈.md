- **后端框架**：FastAPI + Uvicorn (异步 Web 服务)。
- **LLM 集成**：DeepSeek API (通过 `openai` 库兼容调用)，使用 `json_object` 响应格式实现结构化输出。
- **数据处理**：`pydantic-settings` 管理配置，`ruamel.yaml` 处理高保真 YAML 导出，`python-docx`/`pdfplumber` 解析多格式文档。
- **前端技术**：Jinja2 模板引擎 + Tailwind CSS + 原生 JavaScript (处理文件上传与 SSE 监听)。