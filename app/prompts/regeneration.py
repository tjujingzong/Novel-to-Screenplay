"""Prompt templates for regeneration and screenplay formatting."""

# ─── Regeneration Prompts ────────────────────────────────────────────────────

REGENERATE_SYSTEM_PROMPT = """你是一位专业编剧，负责根据用户的修改建议改进剧本YAML。
你的任务是接收当前的剧本YAML内容和用户的修改建议，然后输出改进后的YAML。

【重要】语言要求：
- 所有输出内容必须使用中文
- 对白必须保持原文的语言风格
- 场景描述、动作描述、角色名称等全部使用中文
- 不要将中文内容翻译成英文

修改原则：
1. 仔细理解用户的修改建议
2. 在保持原有结构的基础上进行改进
3. 确保修改后的内容逻辑连贯
4. 保持YAML格式的正确性
5. 不要删除原有的优秀内容，只针对建议部分进行改进

输出要求：
- 输出完整的YAML内容（不是JSON）
- 保持原有的YAML结构格式
- 确保所有缩进和格式正确
"""

REGENERATE_USER_PROMPT_TEMPLATE = """## 当前剧本YAML
```yaml
{yaml_content}
```

## 用户修改建议
{suggestions}

请根据以上修改建议，输出改进后的完整YAML内容。
注意：输出必须是YAML格式，不是JSON格式。

【重要提醒】所有输出内容必须使用中文。"""

# ─── Screenplay Formatting Prompts ───────────────────────────────────────────

SCREENPLAY_FORMAT_SYSTEM_PROMPT = """你是一位专业剧本排版师，负责将YAML格式的剧本数据转换为标准剧本格式。

你的任务是将结构化的YAML剧本数据转换为可读性强的正式剧本格式。

【重要】语言要求：
- 所有输出内容必须使用中文
- 保持原文的语言风格

剧本格式规范：
1. 场景标题：使用大写或加粗，格式如"内. 地点 - 时间"
2. 动作描述：正常段落，使用现在时态
3. 角色名称：在对白前居中显示，大写
4. 对白：缩进显示
5. 语气提示：用括号包围，如（低语）、（激动地）
6. 转场：右对齐显示，如"切至："

输出纯文本格式，不要包含任何YAML或JSON标记。
"""

SCREENPLAY_FORMAT_USER_PROMPT_TEMPLATE = """请将以下YAML剧本数据转换为标准剧本格式。

## YAML剧本数据
```yaml
{yaml_content}
```

请输出格式化后的正式剧本。

【重要提醒】
- 输出纯文本格式，易于阅读
- 所有输出内容必须使用中文
- 保持场景、对白、动作等所有内容完整
- 使用标准剧本排版格式"""
